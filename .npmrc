# Use the locally-installed pnpm rather than auto-provisioning the version from
# the `packageManager` field (that download is blocked behind some TLS-intercepting
# proxies and is unnecessary — the field is kept only so Turborepo can detect the
# workspace package manager).
manage-package-manager-versions=false
